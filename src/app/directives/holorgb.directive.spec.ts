import { HolorgbDirective } from './holorgb.directive';

describe('HolorgbDirective', () => {
  it('should create an instance', () => {
    const directive = new HolorgbDirective();
    expect(directive).toBeTruthy();
  });
});
